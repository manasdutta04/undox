# Social clip scripts (15–30s)

Tag: WeMakeDevs · TrueFoundry · Qodo · #AgentHarness

**Tip:** Film the **dashboard and approval modal**, not invented chat summaries.

## Clip 1 — Approval gate (must post)

**Visual:** TrueForge modal with full name/address/phone/DOB/email (Alex Rivera fixture).  
**VO:** “Undox never submits broker opt-outs until you Allow the exact PII. That’s the harness, not a chatbot.”

## Clip 2 — Sandbox prepare

**Visual:** Tool result showing `prepare_runtime: sandbox-script` for peoplefind/clearbook.  
**VO:** “Prepare isn’t a fake JSON blob — the harness runs the broker prepare script before approval.”

## Clip 3 — Session resume

**Visual:** Kill TrueForge → restart → dashboard / `get_session_state` still shows submitted.  
**VO:** “Kill the server, come back, statuses are still there. Session resume is a first-class beat.”

## Clip 4 — Approval + live dashboard (must post)

**Visual:** Split: TrueForge approval modal (literal PII) left; Undox Exposure Dashboard cards updating right.  
**VO:** “Allow the exact payload — watch risk and broker status update live. Tools and dashboard are the source of truth.”
